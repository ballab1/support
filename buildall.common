#!/bin/bash

declare -r TOP="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"   
cd "$TOP"

source "${TOP}/versions/${OS::-1}" 
source "${TOP}/libs/loadLibraries.sh"

[ $# -gt 0 ] && modules=( "$@" )
build.allContainers "${modules[@]}"
