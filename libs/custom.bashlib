#!/bin/bash

#----------------------------------------------------------------------------------------------
function custom.cbfVersion()
{
    (cd container_build_framework; git rev-parse HEAD)
}
export -f custom.cbfVersion

#----------------------------------------------------------------------------------------------
function custom.io()
{
    curl --insecure "$@"
}
export -f custom.io
