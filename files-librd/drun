docker run -it \
           --rm \
           -e "ZK_HOSTS=$ZK_HOSTS" \
           -e "BOOTSTRAP_SERVERS=$BOOTSTRAP_SERVERS" \
           -e "AVRO_HOSTS=$AVRO_HOSTS" \
           --volume "$PWD:/usr/scr/myapp" \
           --volume "$PWD:/opt" \
           files-librd:20180120 \ 
           bash
